export {
  routeMap,
  landingRouteMap,
  innerPageRouteMap,
} from './utils';

// Экспорт типов
export type {
  // Общие (Combined)
  RouteKey,
  RouteEntry,
  RoutePath,

  // Лендинг (Landing)
  LandingRouteKey,
  LandingRouteEntry,
  LandingRoutePath,

  // Внутренние страницы (Inner Page)
  InnerPageRouteKey,
  InnerPageRouteEntry,
  InnerPageRoutePath,
} from './utils';
